#include<stdio.h>
int main(){
    int r , c;
    scanf("%d",&r);
    scanf("%d",&c);
    int arr[r][c];
    for(int i = 0;i<r;i++){
        for(int j = 0;j<c;j++){
            scanf("%d",&arr[i][j]);
        }
    }
    int max = arr[0][0];
    int second = -1;
    for(int i = 0;i<r;i++){
        for(int j = 0;j<c;j++){
            int salary = arr[i][j];
            if(salary>max){
                second = max;
                max = salary;
            }else if(salary >second && salary < max){
                second = salary;
            }
        }
    }
    printf("Highest Salary : %d\n",max);
    if(second != -1){
        printf("Second Highest Salary : %d",second);
    }
    return 0;
}
