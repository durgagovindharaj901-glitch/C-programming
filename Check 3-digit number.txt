#include <stdio.h>

int main() {
    int num;
    printf("Enter a number: ");
    scanf("%d", &num);
    if(num >= 100 && num <= 999){
        printf("Three-digit number\n");
    }else{
        printf("Not three-digit\n");
    }
    return 0;
}